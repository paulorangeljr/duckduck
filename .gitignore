__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.pytest_cache/
.venv/

# Real auto_register() config — copy duckduck.example.json to duckduck.json
# and fill in your own secret_id / credentials. Never commit the real one:
# an authentication block of type "local" can hold literal passwords/secrets.
duckduck.json

# Catalogs drafted by `generate-catalog` (review them, then promote by hand)
generated_catalog.yaml

# feedback from the web app: users' questions (keep out of git)
feedback.duckdb
feedback.duckdb.wal
feedback_evaluation.json
*.yaml.bak
*.json.bak
